# NOTICE — vendored prior art for ADR-045

These files are retained only as decision evidence for ADR-045, fetched
from upstream at the pinned tag recorded here. They are not part of ranex,
are not imported or executed by it, and carry their upstream licence terms.

- `in-toto-v2.3.0-in_toto_verify.py` — origin: https://github.com/in-toto/in-toto/blob/v2.3.0/in_toto/in_toto_verify.py (in-toto tag `v2.3.0`). License: Apache-2.0 (SPDX-License-Identifier: Apache-2.0).
- `in-toto-v2.3.0-in_toto_run.py` — origin: https://github.com/in-toto/in-toto/blob/v2.3.0/in_toto/in_toto_run.py (in-toto tag `v2.3.0`). License: Apache-2.0 (SPDX-License-Identifier: Apache-2.0).

Both copies passed through a text-converting fetch, so whitespace may differ
from the raw upstream objects (ADR-038 precedent); the digests recorded in
ADR-045 name the bytes actually on disk.
