# Action gates for this repository. Authored, not generated: the contract tree
# holds readiness gates, not action gates — see src/.../yaml/slice_gate_loader.py.
gates:
  - gate_id: landing
    rule_id: TESTS_EXECUTED
    blocking: true
    required_claims:
      - tests-executed
      - contracts-validated
