# Ranex Documentation

Single entry point for everything under `docs/`. Nothing here is runtime-proven:
the architecture is accepted **on paper** (ADR-0003), and neither
`IMPLEMENTATION_START_READY` nor `PRODUCTION_READY` has been declared
(see `architecture/README.md`, opening section, and ADR-0012).

## Who should read what

| You are | Read | Skip |
|---|---|---|
| Owner / non-technical | This file; the opening section of [`architecture/README.md`](architecture/README.md); the Decision sections of [ADR-0001](architecture/decisions/ADR-0001-established-sdlc-governs-ai-work.md) and [ADR-0012](architecture/decisions/ADR-0012-separate-implementation-start-and-production-readiness.md); [`architecture/reviews/README.md`](architecture/reviews/README.md) | Everything else |
| Implementer (before writing code) | The full 14-item order in [`architecture/README.md`](architecture/README.md), with ADR-0007 through ADR-0012 read in full | `assessments/`, `reviews/artifacts/` |
| Anyone checking a specific rule or ID | [`architecture/SDLC_CONTROL_CATALOG.md`](architecture/SDLC_CONTROL_CATALOG.md), [`architecture/SOURCE_OF_TRUTH.md`](architecture/SOURCE_OF_TRUTH.md), `architecture/templates/` | — |

## Directory map

| Path | What it is | Class |
|---|---|---|
| [`architecture/`](architecture/README.md) | Normative core: operating model, control catalog, full-system architecture, source-of-truth policy, AI lifecycle/fleet/artifact specs | CURRENT, normative |
| [`architecture/decisions/`](architecture/decisions/) | ADR-0001 … ADR-0012, all `ACCEPTED`. ADR-0005 and ADR-0006 contain clauses/rows partially superseded by ADR-0011 — each says so in its own header | CURRENT, normative |
| [`architecture/rfcs/`](architecture/rfcs/README.md) | Proposals, not decisions. No RFC exists yet | CURRENT, empty |
| [`architecture/reviews/`](architecture/reviews/README.md) | Review and audit records. Mixed current/superseded/historical — see its README | Mixed; see README |
| `architecture/reviews/artifacts/` | Frozen prompts, raw model outputs, provider metadata, and SHA-256 manifests that pin exact bytes of other documents | HISTORICAL EVIDENCE — never edit, never move |
| `architecture/assessments/` | **Machine-generated** by `scripts/architecture/generate_contracts.py` and `validate_contracts.py` (`COMPLETENESS_REPORT.md` says `GENERATED — RUN VALIDATOR`) | GENERATED — never hand-edit; do not add files here |
| `architecture/templates/` | 40 provisional contract shapes; not executable schemas until gate `AI-G2` passes | CURRENT, reference-only |
| [`research/`](research/README.md) | Research inputs: current evidence bases, machine-consumed registries, and immutable 2026-07-27 historical studies — see its README for per-file status | Mixed; see README |

## Rules that keep this tree trustworthy

- **Generated:** everything under `architecture/assessments/` is regenerated by
  scripts; hand edits will be overwritten or fail validation.
- **Digest-bound:** the `.sha256` manifests under `architecture/reviews/artifacts/`
  freeze exact bytes and paths of many documents (including research files and
  `legal/licensing-manifest.json`). Renaming or moving a listed file breaks the
  evidence chain. `research/ranex-architecture-practice-application-profile.json`
  is additionally pinned by a hardcoded digest inside
  `scripts/architecture/validate_contracts.py`.
- **Historical:** a document dated 2026-07-27/28 that references the retired
  `RANEX_IMPLEMENTATION_GUIDE.md` is readable history under ADR-0002, never
  current instruction.
- **Local-only:** `research/books/` and `research/kimi-research/` are
  git-ignored consultation copies; their public representation is the digest
  manifests under `architecture/reviews/artifacts/`.
