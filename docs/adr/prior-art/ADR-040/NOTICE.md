# Prior-art notice

- `pip-25.2-cli-main.py` — origin: https://github.com/pypa/pip/blob/25.2/src/pip/_internal/cli/main.py (tag 25.2); MIT.
- `pip-25.2-pyproject.toml` — origin: https://github.com/pypa/pip/blob/25.2/pyproject.toml (tag 25.2); MIT.

These files are retained only as decision evidence for ADR-040, fetched
2026-08-30 at the pinned revisions above through this repository's
web-fetch tool (a text conversion of the raw GitHub objects, not `curl`).
The bytes recorded here are the bytes digested in the ADR and may differ in
whitespace from the raw upstream objects. No product code in this
repository is copied from these vendored sources.
